apiVersion: v1
kind: Service
metadata:
  name: truck-fleet
  labels:
    app: truck-fleet
spec:
  selector:
    app: truck-fleet
  ports:
    - name: http
      port: 80
      targetPort: 8080
  type: ClusterIP
